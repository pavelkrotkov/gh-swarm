Adjudicate {repo} PR #{pr_number} only at exact head {head}.
Required adjudication marker: {marker}

Use only the durable GitHub review ledger for this exact head; do not depend on the implementation worktree or local controller state.
Before publishing, re-read the PR and require its full head SHA to remain {head}; otherwise publish nothing and fail this execution attempt.
Publish exactly one PR conversation comment containing the adjudication marker and one machine-readable hermes-swarm-decision-b64 payload. The compact JSON payload must include head_sha={head_repr}, decision=accept|revise, comment_dispositions covering every exact-head finding comment exactly once by github_comment_id with disposition fix|accepted-risk|reject, and required_changes whose source_comment_ids exactly equal the set of fix dispositions. ACCEPT must contain no fix dispositions or required changes. Do not add any semantic round field.
After publishing, re-read GitHub and verify exactly one valid adjudication for {head} is visible and its machine payload decodes back to that same full SHA, decision, and exact finding ledger.
Only then may the Kanban task report success. If publication is not yet visible, keep re-observing within bounded API retries; never claim success first.
If a valid exact-head adjudication already exists, validate it and finish without publishing a duplicate.
