Adjudicate {repo} PR #{pr_number} only at exact head {head}.
Required adjudication marker: {marker}

Use only the durable GitHub review ledger for this exact head; do not depend on the implementation worktree or local controller state.
Before publishing, re-read the PR and require its full head SHA to remain {head}; otherwise publish nothing and fail this execution attempt.
Publish exactly one PR conversation comment containing the adjudication marker and one machine-readable payload in literal form `<!-- hermes-swarm-decision-b64:<base64> -->`. `<base64>` must be the complete URL-safe base64 encoding of compact JSON, never an abbreviation, placeholder, ellipsis, or text outside the marker. The JSON must include head_sha={head_repr}, decision=accept|revise, comment_dispositions covering every exact-head finding comment exactly once by github_comment_id with disposition fix|accepted-risk|reject, and required_changes whose source_comment_ids exactly equal the set of fix dispositions. ACCEPT must contain no fix dispositions or required changes. Do not add any semantic round field.
After publishing, re-read GitHub and require exactly one matching adjudication. Use direct `gh api --jq` plus ordinary shell filters for headless verification; do not use inline interpreter scripts (`python -c`, interpreter heredocs, subprocess wrappers, or equivalents). A supported payload decode path is:
`gh api --method GET --paginate repos/{repo}/issues/{pr_number}/comments --jq '.[] | select(.body | contains("{marker}")) | .body' | grep -o 'hermes-swarm-decision-b64:[A-Za-z0-9_=-]*' | cut -d: -f2- | tr '_-' '/+' | base64 -d | jq -e .`
Decode the complete payload and verify the same full SHA, decision, and exact finding ledger. If Hermes blocks any required verification command, fail this execution attempt immediately with the blocked command and message; do not keep heartbeating or try script variants.
Only then may the Kanban task report success. If validation fails, correct the publication or fail this execution attempt; if publication is not yet visible, keep re-observing within bounded API retries. Never claim success first.
If a valid exact-head adjudication already exists, validate it and finish without publishing a duplicate.
